import { Typography } from "@mui/material";

export default function DefaultFuntion() {
  return (
    <Typography variant="h3">
      Only logged in user should be able to view this page
    </Typography>
  );
}
